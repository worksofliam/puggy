- let mypet = 'cow'
- let isbold = false
mixin pet(name)
  li.pet=name
mixin coolpet(name, bold)
  if bold
    b
      +pet(name)
  else
    +pet(name)
ul
  +coolpet(mypet, isbold)
  +pet('cat')
  +coolpet('dog', true)
  +pet('pig')